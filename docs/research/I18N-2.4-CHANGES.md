# Translation work for the 2.4 content

Follow `docs/research/I18N-TRANSLATE-BRIEF.md` (mechanics, quality bar, glossary, per-language voice; Korean has its own section at the end) and your language's `docs/i18n/glossary-<locale>.md`. Read a few of your language's existing catalogs first (`common.json`, `home.json`, `competitors.json`, `architecture.json`) and keep their terms and tone. Translate from the English files in `src/i18n/locales/en/`.

## What to translate

1. **New file, translate completely:** `aging.json`.
2. **Changed or new keys in existing files.** Update exactly these in your locale's file, leave every other key untouched:
   - `common.json`: `crumbs.aging`, `nav.product.aging.label`, `nav.product.aging.note`, `release.new`
   - `home.json`: the whole `aging` object
   - `images.json`: the whole `aging-decay-path` and `aging-dream-pass` objects. Keys stay English; values are what gets drawn inside a diagram, so keep them as short as the English. Keep `git history`, `restore-page`, `LLM` recognisable (translate "history", keep `git`; keep `restore-page` as is, it is a command).
   - `competitors.json`: `items.mcp-memory-service.same`, `.gain` (all three), `.theyWin`; `items.basic-memory.gain[1]`; `items.openviking.same`, `.theyWin`; `items.agentmemory.same`; `items.supermemory.same`; and the whole new `items.honcho` object
   - `compare.json`: `behind.items` now has SEVEN items. Items 0 and 1 were reworded, a NEW item was inserted at index 2 ("Aging features are unproven"), and the old items 2 to 5 moved to 3 to 6 unchanged. Insert the new one at index 2 in your file; do not retranslate the ones that only moved.
   - `research.json`: `next.recommended.items[0]`, `next.recommended.items[2]`, `next.behind.text`, and the new `priorArt.gave.honcho`
   - `architecture.json`: `retrieval.typedEdges.points[2]` and the new `concurrency.aging`
   - `security.json`: `boundary.items[1].text`

## Terms for this release
Decide these once and add them to your glossary file: memory aging, decay, retention score, half-life, forget-sweep (a scheduled job; keep it a plain descriptive noun), cold page, evict, compact / compaction, near-duplicates, dedup / merge, survivor, superseded (reuse your existing term for supersession), dream pass (a background job where an LLM rewrites old pages while the user is away: translate "dream" literally and naturally, it is meant to evoke sleep), idle, confidence score, contradiction, pinned page, opt-in / off by default. Tier names `working`, `episodic`, `semantic`, `procedural` are product vocabulary: translate them as headings the way your language's developers would, consistently.

Do not translate config keys, tool names or arguments: `[decay.half_life_days]`, `compact_cold_episodic`, `dedup_cold_clusters`, `belief_authority_weight`, `[dream] enabled = true`, `memory_lint`, `memory_query`, `memory_explore`, `memory_read_page`, `memory_status`, `answer: true`, `reasoning`, `pin_first`, `include_related`, `include_superseded`, `explain`, `expires_at`, `restore-page`, `Stale`, `Wrong`, `minimal`, `max`.

## Finish
`cd /mnt/data/Projects/ai-memory-web && node scripts/check-i18n.mjs <locale>` must end with no ERROR lines for your locale and nothing "falling back to English". STALE errors on the keys listed above are expected until they are re-stamped; any OTHER error is yours to fix. Do not run `npm run build`, `--stamp`, or touch any file outside `src/i18n/locales/<locale>/` and `docs/i18n/glossary-<locale>.md`.
Final report under 80 words: term choices for "dream pass", "forget-sweep" and "compaction", and anything you found hard.
